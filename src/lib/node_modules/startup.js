var Odette = require('./global-odette');
module.exports = Odette(this, 'application', '0.0.0', function (shared, scoped) {
    // custom setup code for this version
    // global app is the object that will be shared with all other iframes
}, function () {
    //
});